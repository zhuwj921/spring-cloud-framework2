spring:
  profiles:
    active: dev
  application:
    name: service-user
eureka:
  instance:
    appname: 用户服务
server:
  port: 8530

#mybatis
mybatis-plus:
  mapper-locations: classpath:/mapper/*Mapper.xml
  configuration:
    map-underscore-to-camel-case: true  #开启下划线转驼峰